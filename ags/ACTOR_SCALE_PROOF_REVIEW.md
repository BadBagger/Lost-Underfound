# Lost & Underfound AGS Actor-Scale Proof Review

Generated by `npm.cmd run ags:actor-scale-proofs`.

Contact sheet:

`ags/actor_scale_proofs/contact.png`

## Current Read

The proof is intentionally blunt: admitted engine exports are composited onto
the current AGS plates at their standing points. Characters without admitted
engine exports are red placeholders.

## Findings

| Screen | Finding | Required Action |
| --- | --- | --- |
| `room1/discovery` | No actor standing point on the current screen; clean as a discovery-only stage. | Add a Pip entry/idle proof here if the opening starts visually in this room. |
| `room1/clerk` | Pip placement reads usable. Bramble now uses the actual desk-clerk cutout, moved right into the chair/desk station, with the desk-front polygon occluding the lower body. | Final Bramble desk animation should be built from this placement: face, bow, and hands above the desktop; lower body hidden by the counter front. |
| `room1/gate` | Pip and Bottlecap are visible and not buried by the gate. | Confirm Bottlecap’s final gate baseline once the AGS walk-behind is authored. |
| `room2/concourse` | Pip, Bramble, and Scuttle positions are readable, but current Pip art is still visually flatter than the painted backdrop. | Replace/upgrade Pip with the Meshy-derived gameplay export once admitted. |
| `room2/annex-threshold` | Grommet is correctly blocked as missing production export. | Admit Grommet production frames before this screen can be final. |
| `room2/audience-chamber` | Chairman Toggle is correctly blocked as missing production export. | Admit Toggle production frames before this screen can be final. |
| `room3/annex-interior` | Grommet and Toggle are missing; Pip/Bramble placement is readable. | Add admitted Grommet and Toggle exports, then tighten marble candidate positions. |
| `room3/escape-concourse` | Grommet guardian block is missing; Pip/Bramble/Scuttle placement reads. | Grommet guardian-block animation is the gating asset. |
| `room3/grate-revisit` | Grommet is missing; Pip, Bramble, and Bottlecap cluster close enough to judge goodbye staging. | Add Grommet and review crowd spacing so the goodbye does not become a pile-up. |

## Production Rule

A screen cannot move from `visualReview.status: intake` to `approved` until its
actor-scale proof uses admitted production exports for every required actor and
the composite has been visually reviewed.
